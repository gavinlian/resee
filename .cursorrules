# 族谱 App — AI 规则

你是一个精通 uni-app (Vue3 + Vite + TS) 和 UniCloud (Node.js + NoSQL) 的全栈专家。所有开发活动必须严格遵循以下规则。

---

## 【语言规范】

1. 思考、回复、代码注释全部使用简体中文
2. 前端和后端代码都必须使用 TypeScript

---

## 【前端规范 — 代码】

| 规则 | 说明 |
|------|------|
| **模板语法** | 禁止使用 HTML 标签（div/span/img/a），必须用 `<view>` `<text>` `<image>` `<navigator>` |
| **API 调用** | 禁止使用浏览器原生 API（document/window/localStorage/fetch/axios），必须用 `uni.xxx` |
| **脚本语法** | 必须用 Vue3 `<script setup lang="ts">`，组合式 API |
| **样式** | 必须用 scoped，尺寸单位用 rpx，禁止在 JS 中操作 DOM |
| **路由跳转** | 禁止用 vue-router，必须用 `uni.navigateTo` / `uni.switchTab`；禁止在模板事件中直接调用 `uni.navigateTo`，必须在 script 中封装成函数再调用（否则 Vite 压缩后 uni 对象会找不到） |
| **生命周期** | 必须用 uni-app 页面生命周期（`onLoad` / `onShow` / `onReady`），禁止用纯 Web 的 `mounted` |
| **条件编译** | 多端差异必须用 `#ifdef H5` / `#ifdef MP-WEIXIN` / `#ifdef APP-PLUS` |

---

## 【前端规范 — H5 兼容性】

| 规则 | 说明 |
|------|------|
| **兼容性** | 必须兼容 iOS Safari、微信浏览器、安卓 WebView |
| **输入框** | input/textarea 必须用内联样式确保可交互：`flex:1;min-width:0;border:none;background:transparent;outline:none` |
| **触摸延迟** | 触摸事件加 300ms 延迟防穿透 |
| **字体** | 字体最小 12px |
| **异步** | 异步请求必须 try/catch + 超时（5-10s） |
| **deprecated** | 禁止用已废弃的 Web API |

---

## 【后端 UniCloud 规范】

1. 使用"云对象"编写，不使用传统"云函数"
2. 数据库操作用 JQL 或 `db.collection` 链式调用，**禁止写 SQL**
3. 用 `uniCloud.database()` 的 `dbCmd` 做条件查询
4. 第三方库用 `npm install` 后正确 import

---

## 【安全红线】

- 不硬编码密钥、token、password、apiKey
- 不在日志打印敏感信息
- 不确定的操作先问用户

---

## 【项目结构】

```
e:\resee\uiapp\
│
├── src/                          — 前端源码
│   ├── pages/                    — 页面（pages.json 中注册）
│   ├── components/               — 组件（大写 PascalCase）
│   ├── api/                     — API/AI 服务
│   ├── utils/                   — 工具函数（db.ts / ai-config.ts 等）
│   ├── styles/                  — 全局样式
│   ├── pages.json                — uni-app 路由配置
│   ├── manifest.json             — uni-app 应用配置
│   └── App.vue                   — 根组件
│
├── uniCloud-aliyun/               — 后端 Unicloud
│   ├── cloudfunctions/          — 云函数
│   │   ├── ai-ocr/              — OCR 识别
│   │   ├── ai-parse/            — AI 解析族谱
│   │   ├── ai-search/           — AI 搜索
│   │   └── export-pdf/         — PDF 导出
│   └── database/                 — 数据库 schema
│
├── static/                        — 静态资源（TabBar 图标等）
├── docs/                          — 文档（PRD / 设计稿）
├── package.json                   — 前端依赖
├── vite.config.ts                 — Vite 配置
├── tsconfig.json                  — TypeScript 配置
├── pages.json                     — uni-app 路由配置
├── manifest.json                  — uni-app 应用配置
├── index.html                     — H5 入口
├── .cursorrules                   — 本文件
└── .cursoraicignore               — AI 忽略文件
```

---

## 【云函数目录规范】

每个云函数一个独立目录，位于 `uniCloud-aliyun/cloudfunctions/` 下：

```
uniCloud-aliyun/cloudfunctions/<函数名>/
├── index.js      — 云函数入口
├── package.json  — 依赖
└── README.md     — 说明（复杂函数需写）
```

---

## 【命名规范】

| 类型 | 格式 | 示例 |
|------|------|------|
| 文件/目录 | kebab-case | `ai-parse`、`ocr-review` |
| 页面目录 | 小写 | `pages/index`、`pages/tree` |
| 组件 | PascalCase | `GeneTreeChart.vue` |
| 函数 | camelCase | `getAILimits` |
| 常量 | UPPER_SNAKE_CASE | `COLLECTIONS` |